spring:
  datasource:
    url: jdbc:mysql://localhost:3306/elearningdb
    username: root
    password: Admin123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8090

application:
  config:
    cloudinary-url: http://localhost:8096/api/cloudinary
    category-url: http://localhost:8096/api/category